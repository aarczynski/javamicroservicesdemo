# Base for k8s-cluster/manifests/overlays/minikube/observability — see
# candidates/kustomization.yaml for why this exists. values-*.yaml files are
# Helm values, not plain resources, so they're not listed here.
resources:
  - namespace.yaml
  - dashboards-configmap.yaml
