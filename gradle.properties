otelJavaAgentVer=2.31.1
# The OTel SDK core version bundled inside opentelemetry-javaagent 2.31.1 (confirmed live via
# that agent's own target_info{telemetry_sdk_version} resource attribute) - otel-metrics-filter
# compiles against these interfaces only (compileOnly), must match what the agent actually
# provides at runtime or the extension jar's classes won't resolve against the agent's own.
otelSdkVer=1.65.0
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
